[build]
buildCommand = "npm run build -w server && test -f server/dist/index.js"

[deploy]
startCommand = "node server/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
