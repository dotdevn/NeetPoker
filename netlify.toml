[build]
base = "."
command = "npm install && npm run build -w dashboard"
publish = "dashboard/dist"

[build.environment]
NODE_VERSION = "22"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
